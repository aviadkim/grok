{
  "name": "grok",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "cd backend && npm install && cd ../frontend && npm install && npm run build && cd .. && node copy-build.js",
    "start": "cd backend && PORT=10000 NODE_ENV=production node server.mjs"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.4.7",
    "openai": "^4.85.4",
    "portfinder": "^1.0.32",
    "langchain": "^0.1.0",
    "faiss-node": "^0.5.1"
  }
}
